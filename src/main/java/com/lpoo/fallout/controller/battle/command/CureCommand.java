package com.lpoo.fallout.controller.battle.command;

import com.lpoo.fallout.controller.Command;

public class CureCommand implements Command<NullData> {
    @Override
    public void activate(NullData requestData) {

    }
}
